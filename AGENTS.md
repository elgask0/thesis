# Agent Bootstrap

This file is also temporary.

It exists only so Claude can replace it after auditing the repo.

## Default Rule

Any agent working in this repo should begin as an **auditor**, not as an implementer.

Before setting up workflows, roles, or code plans:

1. read the repo
2. find contradictions or unclear assumptions
3. ask Mario the important questions
4. then design the real agent setup

## Interim Expectations

Until Claude replaces this file, assume:

- the repo is an early scaffold
- there is no real code yet
- there are no committed datasets yet
- source discovery is not the same as data feasibility
- the first practical data tasks are:
  - CO2 backbone
  - Gansu electricity source mapping

## What The Final Version Should Contain

After Claude's repo audit, the rewritten `AGENTS.md` should define:

- the real agent roles
- the canonical docs
- handoff rules
- unresolved project decisions
- what is core versus extension
- what should happen next

## Anti-Patterns

Do not:

- invent finished pipelines
- overstate data coverage
- design around hourly electricity data that do not exist
- turn speculative extensions into baseline tasks

## Replacement Instruction

Claude should rewrite this file only **after** reading the repo and producing a first inconsistency / clarification memo for Mario.
